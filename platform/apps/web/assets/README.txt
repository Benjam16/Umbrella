Optional large assets for the Next.js site (e.g. background video).

Place files here and import from src/ or use /assets/... in public/ if you
move them under public/ for static URLs.

Default build uses CSS gradients only — no binary assets required.
