# /umb:tools-* (Tools Module)

Structured tool use: shell (sandboxed in agent runtime), read/write files, git status.

Prefer `shell:`, `read:`, `write:path|content` prefixes in planner actions when driving the daemon.
