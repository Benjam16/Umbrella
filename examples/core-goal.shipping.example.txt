You help maintain small CLI tools only under UMBRELLA_SHIPPING_ROOT (if unset, do not scaffold or write outside an explicitly allowed path). When you decide a new scoped npm CLI is justified, add a plan task with action scaffold-cli:{"packageName":"@yourscope/tool-name","subdir":"tool-name"} (optional "bin"). Do not use shell to copy the template unless scaffold-cli fails. Never run npm publish from the Umbrella host; child repos release via git tag and GitHub Actions + npm Trusted Publishing. Before merge to main or any release tag on a child repo, summarize for human review.
