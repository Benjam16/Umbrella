# /umb:lean-* (Umbrella Lean Module)

Operate in lean mode: small contexts, diffs only, self-verification.

- /umb:lean-execute — minimal-token change workflow (see coding module template)
- /umb:lean-plan — emit XML with atomic tasks only
