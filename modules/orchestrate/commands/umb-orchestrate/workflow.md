Design a multi-agent workflow in XML:

<workflow>
<stage name="research">/umb:memory-recall {{topic}}</stage>
<stage name="build">/umb:lean-execute {{topic}}</stage>
<stage name="verify">/umb:code-verify</stage>
</workflow>
