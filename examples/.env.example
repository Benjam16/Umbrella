# Copy to ~/.umbrella/.env (or set UMBRELLA_DOTENV to an absolute path).
# Loaded when the daemon starts, before config.json. Does not override existing shell env.

# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# UMBRELLA_DASHBOARD_PORT=4578
# TELEGRAM_BOT_TOKEN=
# UMBRELLA_INBOUND_SECRET=
